"""
API v1 routers package.

Each module in this package defines an :class:`~fastapi.APIRouter` for a
specific group of related endpoints (e.g. :mod:`app.api.v1.routers.health`).

Use this package to add new domain routers (for example: `orbital`,
`coordinate`, `time`, etc.).
"""

# Exported router modules live under `app.api.v1.routers`.
__all__ = []
